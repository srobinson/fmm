{
  "tasks": [
    {
      "id": "symbol-lookup-1",
      "prompt": "Which file exports the symbol `BenchmarkOrchestrator`? Reply with ONLY the file path, nothing else.",
      "ground_truth": "bench/benchmark.ts",
      "scoring": "exact_path"
    },
    {
      "id": "symbol-lookup-2",
      "prompt": "Which file exports the symbol `AdaptiveProxy`? Reply with ONLY the file path, nothing else.",
      "ground_truth": "agentic-flow/src/proxy/adaptive-proxy.ts",
      "scoring": "exact_path"
    },
    {
      "id": "symbol-lookup-3",
      "prompt": "Which file exports the symbol `BillingSystem`? Reply with ONLY the file path, nothing else.",
      "ground_truth": "agentic-flow/src/billing/index.ts",
      "scoring": "exact_path"
    },
    {
      "id": "deps-list-1",
      "prompt": "List ALL local file dependencies of `agentic-flow/src/proxy/adaptive-proxy.ts` (the local files it imports from). Reply with ONLY the list of dependency paths, one per line, nothing else.",
      "ground_truth": [
        "../utils/logger.js",
        "./anthropic-to-gemini.js",
        "./http2-proxy.js",
        "./http3-proxy.js",
        "./websocket-proxy.js"
      ],
      "scoring": "set_match"
    },
    {
      "id": "deps-list-2",
      "prompt": "List ALL local file dependencies of `packages/agentdb/src/services/auth.service.ts` (the local files it imports from). Reply with ONLY the list of dependency paths, one per line, nothing else.",
      "ground_truth": [
        "../security/input-validation.js",
        "../utils/crypto.utils.js",
        "./token.service.js"
      ],
      "scoring": "set_match"
    },
    {
      "id": "reverse-deps-1",
      "prompt": "Which files import from `packages/agentdb/src/middleware/auth.middleware.ts`? List ALL files that depend on it. Reply with ONLY the file paths, one per line, nothing else.",
      "ground_truth": [
        "packages/agentdb/src/middleware/rate-limit.middleware.ts",
        "src/api/index.ts",
        "tests/security/auth-middleware.test.ts"
      ],
      "scoring": "set_match"
    },
    {
      "id": "reverse-deps-2",
      "prompt": "Which files import from `packages/agentdb/src/utils/crypto.utils.ts`? List ALL files that depend on it. Reply with ONLY the file paths, one per line, nothing else.",
      "ground_truth": [
        "packages/agentdb/src/services/auth.service.ts",
        "packages/agentdb/src/services/token.service.ts",
        "packages/agentdb/src/tests/unit/auth/crypto.utils.test.ts"
      ],
      "scoring": "set_match"
    },
    {
      "id": "imports-list-1",
      "prompt": "What external packages does `packages/agentdb/benchmarks/benchmark-runner.ts` import? Reply with ONLY the package names, one per line, nothing else.",
      "ground_truth": ["fs", "path", "perf_hooks"],
      "scoring": "set_match"
    },
    {
      "id": "imports-list-2",
      "prompt": "What external packages does `agentic-flow/src/proxy/http2-proxy.ts` import? Reply with ONLY the package names, one per line, nothing else.",
      "ground_truth": ["crypto", "fs", "http2"],
      "scoring": "set_match"
    },
    {
      "id": "export-count-1",
      "prompt": "How many symbols does `packages/agentdb/src/services/auth.service.ts` export? Reply with ONLY the number, nothing else.",
      "ground_truth": "20",
      "scoring": "exact_number"
    }
  ]
}
