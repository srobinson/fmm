name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    # PR: always run. Push to main: only on direct pushes (skip merged PRs and release-please).
    if: >-
      github.event_name == 'pull_request'
      || (github.event.head_commit.committer.username != 'web-flow'
          && !contains(github.event.head_commit.message, 'chore(main): release'))
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Install nextest
        uses: taiki-e/install-action@nextest

      - name: Test
        run: cargo nextest run --workspace

      - name: Doctests
        run: cargo test --workspace --doc

      - name: Build (release)
        run: cargo build --workspace --release

      - name: Check generated docs are in sync with tools.toml
        run: |
          git diff --exit-code \
            crates/fmm-cli/src/mcp/generated_schema.rs \
            crates/fmm-cli/src/cli/generated_help.rs \
            crates/fmm-cli/templates/SKILL.md || \
            (echo "ERROR: Generated docs are out of sync with tools.toml. Run 'just gen-docs' and commit the result." && exit 1)
